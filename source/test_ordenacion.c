#include <stdio.h> 
#include <stdbool.h> 

/* Incluimos el fichero de las cabezeras de los procedimientos */
#include "../include/headers.h"

void test_ordenacion(int vector[], int n_columnas)
{
    int largo_tabla[] = {1, 10, 50, 100, 1000, 2500, 5000, 10000, 25000, 50000, 100000}; // Largo del vector que se prueba
    int intercambios[3][11]; // tabla donde se guarda los contadores de los sistemas de ordenacion


}